apiVersion: v1
kind: Service
metadata:
  name: postgres-internal
  namespace: game-server
spec:
  # StatefulSetが安定したPod名を使えるよう、PodのIPを直接解決するServiceにする。
  clusterIP: None
  selector:
    app: postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: postgres-tcp
